
  # Landing page para clínica

  This is a code bundle for Landing page para clínica. The original project is available at https://www.figma.com/design/IsNtGLF64dMmYmPBldsul1/Landing-page-para-cl%C3%ADnica.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  
 Run `npm run build` to start the build.

  Author: Gui Chaves
